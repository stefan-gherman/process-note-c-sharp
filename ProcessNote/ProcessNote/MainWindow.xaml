<Window x:Class="ProcessNote.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Task Manager Monitor" Height="450" Width="800" Loaded="Window_Loaded_1">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <!--<ColumnDefinition Width="auto"/>-->

            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5" />
            <!--<RowDefinition Height="auto" />-->

            <RowDefinition Height="*" />
            <RowDefinition Height="5" />
        </Grid.RowDefinitions>


        <ListView Grid.Column="1" Grid.Row="1"
                  Background="DarkGray"
                  Margin="10" 
                  Name="statsSource"
                  >
            <ListView.View>

                <GridView>
                    <GridViewColumn Header="ID" Width="60" DisplayMemberBinding="{Binding ID}"/>
                    <GridViewColumn Header="Name" Width="150" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Note" Width="100" DisplayMemberBinding="{Binding Note}"/>
                    <GridViewColumn Header="CPU" Width="60" DisplayMemberBinding="{Binding CPU}"/>
                    <GridViewColumn Header="Memory" Width="100" DisplayMemberBinding="{Binding Memory}"/>
                    <GridViewColumn Header="Start Time" Width="160" DisplayMemberBinding="{Binding Started}"/>
                    <GridViewColumn Header="Thread" Width="100" DisplayMemberBinding="{Binding Thread}"/>

                </GridView>


            </ListView.View>

        </ListView>
        <CheckBox Content="Always on Top" FontSize="10" Grid.Column="1" HorizontalAlignment="Right"  VerticalAlignment="Top" Width="Auto" Grid.Row="1" Margin="0,0.2,42.8,0" Click="CheckBox_Click" />
        <Button Content="Search Online" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Top" Grid.Column="1" Margin="0,0.2,143.8,0" Height="Auto" Width="Auto" Grid.Row="1"/>
    </Grid>
</Window>
